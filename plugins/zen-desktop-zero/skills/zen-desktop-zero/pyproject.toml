[project]
name = "desktop-zero"
version = "0.1.0"
description = "Desktop zero workflow runtime"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
desktop-zero = "desktop_zero.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/desktop_zero"]
